---
description: 自开发组件 setting.vue（设置面板）生成规则 - 双端版
globs:
  - "web/src/form-component/form-editor/*-setting.vue"
alwaysApply: true
---

# setting.vue（设置面板）生成规则 - 双端

> **⚠️ 本文件是双端项目 setting.vue 生成的权威文档，优先级高于 apaas-form-component-dual-dev.mdc 里任何涉及 setting.vue 的老示例。**

## 🛑 硬性 DON'T（严禁违反，违反必 build 失败或数据丢失）

### setting.vue 主体只允许使用以下标签

**✅ 允许**：
- `form-custom-input-editor` / `form-custom-select-editor` / `form-custom-textarea-editor` / `form-custom-switch-editor`（scaffold 预置原子）
- `form-custom-field-assign-editor` / `form-custom-table-field-assign-editor`（赋值 editor）
- 你按"🛑 自造业务 editor 强制约定"小节自建的 `form-custom-{业务名}-editor`

**🔴 严禁**在 setting.vue 主体里直接使用：

| 严禁 | 为什么错 | 正确替代 |
|---|---|---|
| `<form-custom-sechma-item>` | **只是容器**，不提供 formValue 双向绑定；在 setting.vue 主体里直用后，你只能手写 `<el-date-picker v-model="formValue" />` 这种错误代码 —— setting.vue 本体**没有 formValue**，值会掉到 phantom property，**UI 正常但数据完全不存**。sechma-item 仅允许用在业务 editor（`web/src/form-component/form-editor/components/form-custom-{name}-editor.vue`）的 template 里作为外壳。 | 抽成业务 editor（按下方"自造业务 editor 铁则"写），setting.vue 里只用 `<form-custom-{name}-editor label="..." property="..." v-bind="$props" />` |
| `<el-form-item>` | 同上（平台外层已提供表单容器） | 用 form-custom-*-editor（内部已包含 el-form-item） |
| `<el-input>` / `<el-input type="textarea">` | 没有双向绑定到 `componentConfig[configProperty][property]`，数据丢失 | `<form-custom-input-editor>` / `<form-custom-textarea-editor>` |
| `<el-select>` + `<el-option>` | 同上 | `<form-custom-select-editor :options="[]">` |
| `<el-switch>` | 同上 | `<form-custom-switch-editor>` |
| `<el-radio-group>` + `<el-radio>` | 同上 | `<form-custom-select-editor>`（单选即下拉） |
| `<el-checkbox-group>` + `<el-checkbox>` | 同上 | `<form-custom-select-editor :multiple="true">` |
| `<el-date-picker>` / `<el-time-picker>` 直接用 | 没有双向绑定，数据丢失 | 抽成业务 editor（参考"自造业务 editor 强制约定"），不要在 setting.vue 主体里内联 |
| `<el-form>`（最外层不要包） | 平台外层已提供表单容器 | 直接用 `<div class="form-custom-{name}-setting">` 包裹 |
| 内联 `v-model="formValue"` 在 setting.vue 主体的插槽/元素上 | **setting.vue 本体没有 formValue** —— `formValue` 是 `FormEditorMixin` 提供给**业务 editor 组件**的，setting.vue 只用 `EditorFormConfigMixin`（无 formValue），写 `v-model="formValue"` 完全不生效 | 任何需要"UI + 双向绑定"的场景都抽成业务 editor，setting.vue 里只做组件组装 |

### 其他严禁

**严禁**调用不存在的 API：`formEngine.updateWidgetConfig()`、`formEngine.updateCustomComponentConfig()`、`formEngine.setWidgetInfo()` 等。

## 何时需要生成

当组件有"设计器中可配置的属性"时，在 **`web/src/form-component/form-editor/`** 下生成 `{name}-setting.vue`（**只在 web 端生成**，设计器配置面板不跑在 mobile），并同步写 `{name}.editor.config.json`（详见 editor-config.mdc）。

## 基础设施（scaffold 已预置，直接 import 即可，不要重写）

### Mixin
- `@shared/mixin/form-editor.mixin.js`
  — 提供 `formValue` 双向绑定、`rules` 自动拼装、标准 props（label / property / showRequired / placeholder / help / i18nString 等）

### 通用 editor 原子
- `./components/form-custom-sechma-item.vue` — label + help tooltip + 校验容器
- `./components/form-custom-input-editor.vue` — 单行输入（el-input）
- `./components/form-custom-select-editor.vue` — 下拉（x-select，需传 options）
- `./components/form-custom-textarea-editor.vue` — 多行输入
- `./components/form-custom-switch-editor.vue` — 开关

### 字段赋值 editor（组件把派生值写给其他字段的场景）
- `./components/form-custom-field-assign-editor.vue` — 主表字段赋值（让用户把**组件产出的源字段**映射到表单里的目标字段）
- `./components/form-custom-table-field-assign-editor.vue` — 子表字段赋值（子表级别映射 + 子表内字段映射）

### 工具 & 常量
- `./utils/form-item.util.js` — 7 个 formItemList 遍历工具（如 `getAllFormItemListExcludeTable`、`getAllSonTableFormItemList`）
- `./constant/form-component-types.js` — FORM_* 组件类型常量（`INCLUDE_COMPONENT_TYPE` / `EXCLUDE_SHOW_FIELD_TYPE` 等）

**不要**重新生成上述文件。业务性子组件（如 `form-custom-card-list` / `request-header-form`）才按需生成。

## 路径约定（双端关键）

- **必须**使用 `@shared/mixin/...`，**不要**用 `@/mixin/...`
- `@shared` 指向 `../shared`，是双端共享代码
- editor 原子组件内部已经用 `@shared/mixin/form-config.mixin` 和 `@shared/mixin/form-editor.mixin`，直接用即可

## setting.vue 骨架

```vue
<template>
  <div class="form-custom-demo-setting">
    <form-custom-select-editor
      label="显示字段"
      property="showField"
      v-bind="$props"
      :showRequired="true"
      :options="fieldOptions"
      :help="['组件展示的字段']"
    ></form-custom-select-editor>

    <form-custom-input-editor
      label="占位文本"
      property="placeholder"
      v-bind="$props"
      :help="['为空时的提示文字']"
    ></form-custom-input-editor>

    <form-custom-switch-editor
      label="可搜索"
      property="searchable"
      v-bind="$props"
    ></form-custom-switch-editor>
  </div>
</template>

<script>
import EditorFormConfigMixin from '@shared/mixin/form-config.mixin';
import FormCustomInputEditor from './components/form-custom-input-editor.vue';
import FormCustomSelectEditor from './components/form-custom-select-editor.vue';
import FormCustomSwitchEditor from './components/form-custom-switch-editor.vue';

export default {
  name: 'FormComponentDemoSetting', // ⚠️ 必须与 editor.config.json 的 componentName 完全一致
  mixins: [EditorFormConfigMixin],
  components: { FormCustomInputEditor, FormCustomSelectEditor, FormCustomSwitchEditor },
  computed: {
    fieldOptions() {
      return [{ value: 'name', label: '名称' }];
    },
  },
};
</script>
```

## EditorFormConfigMixin 注入的核心属性

| 属性 | 用途 |
|---|---|
| `this.componentConfig` | 整个组件的配置对象 |
| `this.componentConfig.customComponentConfig[property]` | **所有用户输入值存这里** |
| `this._validate` | 传给弹窗子表单做校验 |
| `this.formItemList` | 表单所有字段列表（字段映射场景） |
| `this.globalData.appId` | 当前应用 ID（拉平台变量/字典时用） |
| `$props` | editor 原子组件必须 `v-bind="$props"` 透传 |

## 数据存取

- **读**：`this.componentConfig.customComponentConfig.<property>`
- **写**（数组需保证响应式）：
  ```javascript
  const list = [...(this.componentConfig.customComponentConfig.xxx || [])];
  list.push(item);
  this.$set(this.componentConfig.customComponentConfig, 'xxx', list);
  ```
- **禁止**用 `this.widget.xxx`（那是运行时属性，setting.vue 里读不到）

## 弹窗模式（复杂配置场景）

当配置项需要独立子表单（如 header 列表、字段映射）：

```vue
<form-custom-sechma-item label="请求头" property="headers" v-bind="$props">
  <div class="add-card-button" @click="headerModalVisible = true">添加请求头</div>
  <form-custom-card-list
    deleteModalTitle="删除请求头"
    deleteModalMessage="是否确定删除请求头？"
    :card-list="componentConfig.customComponentConfig.headers"
  ></form-custom-card-list>
  <x-modal
    title="添加请求头"
    :append-to-body="true"
    :destroy-on-close="true"
    :customModalBg="true"
    :modal="false"
    :okConfig="headerOkConfig"
    :cancelConfig="cancelConfig"
    :modalVisible.sync="headerModalVisible"
    width="middle"
  >
    <request-header-form
      ref="headerFormRef"
      :formItemList="formItemList"
      :_validate="_validate"
      :componentConfig="componentConfig"
    ></request-header-form>
  </x-modal>
</form-custom-sechma-item>
```

```javascript
data() {
  return {
    headerModalVisible: false,
    cancelConfig: { title: this.$t('common.cancel') },
    headerOkConfig: {
      title: this.$t('common.confirm'),
      onOk: () => {
        this.$refs.headerFormRef.validator().then(() => {
          const list = [...(this.componentConfig.customComponentConfig.headers || [])];
          list.push(this.$refs.headerFormRef.header);
          this.$set(this.componentConfig.customComponentConfig, 'headers', list);
          this.headerModalVisible = false;
        }).catch(() => console.error('校验失败'));
        return true; // ⚠️ return true = 阻止 x-modal 自动关闭，由 onOk 内部决定关闭时机
      },
    },
  };
}
```

## 联动显示

```vue
<form-custom-switch-editor label="分页" property="pagination" v-bind="$props" />
<form-custom-input-editor
  v-if="componentConfig.customComponentConfig.pagination"
  label="每页条数"
  property="pageSize"
  v-bind="$props"
/>
```

## 网络请求（拉平台变量/字典）

```javascript
created() {
  this.queryVariableList();
},
methods: {
  queryVariableList() {
    this.$request({
      url: '/xdap-app/variable/query/variableDetails',
      method: 'get',
      disableSuccessMsg: true,
      params: { appId: this.globalData.appId, page: 1, pageSize: 1000 },
    }).asyncThen(
      (resp) => { this.variableList = resp.table; },
      (err) => console.error(err),
    ).asyncErrorCatch((err) => console.error(err));
  },
}
```

## ⚠️ 配置项完整性（必须与 brainstorm 方案 1:1 对齐）

setting.vue 里的配置项**必须严格对齐 brainstorm "设计方案确认"中"配置项"表格**：表格列了 N 条 property，setting.vue 就要有 N 个对应的 `form-custom-*-editor` 节点，**每条 property 都要有独立的 editor 节点**。

**严禁**：
- 省略任何配置项（哪怕觉得"暂时可以先跳过"或"实现起来麻烦"）
- 修改 property 名、数据类型、UI 渲染 editor 类型
- 合并或重排多个配置项

**brainstorm 里标了自造业务 editor（如 `form-custom-date-time-editor` 等非预置原子）的配置项**：
1. **先**按下方"🛑 自造业务 editor 的强制约定"新建该业务 editor 的 vue 文件（路径 `web/src/form-component/form-editor/components/form-custom-xxx-editor.vue`）
2. **再**在 setting.vue 里 import 并使用它
3. **严禁**因为"业务 editor 复杂"就跳过对应配置项——brainstorm 方案里的每条配置项都是硬契约

**build 前自检**：逐条核对 brainstorm 配置项表格与实际 setting.vue：
- 每个 property 名 → setting.vue 里能找到 `<form-custom-xxx-editor property="{同名}" ...>`
- 标了业务 editor 的行 → `web/src/form-component/form-editor/components/form-custom-xxx-editor.vue` 真实存在

## ⚠️ setting.vue 完整注册清单（缺一不可，否则平台加载不到设置面板）

生成 `{name}-setting.vue` 后**必须同时完成以下两处注册**。只改其中一个是 half-register，用户打开属性面板空空如也。

**注册 1**：`{name}.editor.config.json` → 追加到 `web/src/form-component-config/form-editor/index.js`
```js
import FormComponentDemoEditorConfig from './form-component-demo.editor.config.json'
const editorConfigList = [FormComponentDemoEditorConfig]
export default editorConfigList
```

**注册 2**：`{name}-setting.vue` 本身 → 追加到 `web/src/form-component/form-editor/index.js`（⚠️ **最容易漏**）
```javascript
import FormComponentDemoSetting from './form-component-demo-setting.vue';
const customFormEditorList = [FormComponentDemoSetting];
export default customFormEditorList;
```

两个 index.js 的职责区分：
- `form-component-config/form-editor/index.js` = 平台注册的 JSON 清单（聚合 `.editor.config.json`），导出 `editorConfigList`
- `form-component/form-editor/index.js` = 实际的 Vue 组件清单（聚合 `.vue`），导出 `customFormEditorList`

## ⚠️ widget.config.json 在双端项目里只有一份（严禁双写）

双端项目的 `widget.config.json` **只在 `shared/widget.config.json`**（scaffold 已预置，LLM 只修改内容不新建文件）。

**严禁**：
- 在 `web/src/form-component-config/form-widget/` 下新建 `*.widget.config.json`
- 在 `mobile/src/form-component-config/form-widget/` 下新建 `*.widget.config.json`

web 和 mobile 两端的 `form-component-config/form-widget/index.js` 都已经 `import XxxWidgetConfig from '@shared/widget.config.json'`，保持原样不要改。

## 字段赋值场景（组件把派生值写给其他字段）

### 何时需要用字段赋值 editor
任意下面一种场景，都要用 `form-custom-field-assign-editor` 或 `form-custom-table-field-assign-editor`（而不是靠 aPaaS 字段联动规则等外部机制）：
- 数据选择类组件：选中一行数据后把多个字段回填到当前表单
- **派生值输出**：组件自身算出的派生值（如时间差、总分、金额合计）需要写到其他字段
- 联动赋值：组件自身变化时自动更新其他字段

简单场景直接把赋值配置写在 `{name}-setting.vue` 里；规则多且复杂时（数据选择类）可独立一个 `{name}-assign.vue` 作为第二个 Tab。注意：setting.vue 和 assign.vue 都**只放 web 端**（`web/src/form-component/form-editor/`）。

### 核心概念：`otherFields` 不是"表单里其他字段"

**`otherFields` 是"组件作为赋值源能产出的字段列表"**（组件侧的**源字段**），不是"当前表单里除自身外的字段"。用户在 UI 里会为每个源字段选一个真实表单目标字段；editor 内部会按 `componentType` 过滤，只让用户选同类型的真实字段。

`otherFields` 每个元素结构：

```javascript
{
  uuid: string,          // 组件内部逻辑 key（自行编造的业务语义 id，不是表单真实字段 uuid）
  label: string,         // 中文展示名
  componentType: string, // 按派生值的数据类型选（见下表）
}
```

**componentType 映射表**（按派生值的业务类型选）：

| 派生值类型 | componentType |
|---|---|
| 数字 | `FORM_NUMBER_INPUT` |
| 字符串 | `FORM_TEXT_INPUT` |
| 金额 | `FORM_MONEY_INPUT` |
| 中国大陆手机号 | `FORM_PHONE_INPUT` |
| 邮箱 | `FORM_EMAIL_INPUT` |
| 中国大陆证件号 | `FORM_IDCARD_INPUT` |

### 完整示例：时间差组件的 setting.vue

组件功能：用户选起止时间，组件算出时间差（毫秒），把差值赋值到用户在 setting 里指定的目标字段。

```vue
<template>
  <div class="form-custom-date-time-range-setting">
    <form-custom-input-editor label="起始时间" property="startTime" v-bind="$props" />
    <form-custom-input-editor label="结束时间" property="endTime" v-bind="$props" />

    <form-custom-field-assign-editor
      label="时间差赋值"
      property="diffAssign"
      v-bind="$props"
      :otherFields="diffSourceFields"
      :help="['把组件算出的时间差赋值到指定字段']"
    ></form-custom-field-assign-editor>
  </div>
</template>

<script>
import EditorFormConfigMixin from '@shared/mixin/form-config.mixin';
import FormCustomInputEditor from './components/form-custom-input-editor.vue';
import FormCustomFieldAssignEditor from './components/form-custom-field-assign-editor.vue';

export default {
  name: 'FormComponentDateTimeRangeSetting',
  mixins: [EditorFormConfigMixin],
  components: { FormCustomInputEditor, FormCustomFieldAssignEditor },
  computed: {
    diffSourceFields() {
      // 组件自身产出的源字段列表。时间差组件只输出一个"时间差"派生值，所以只有一项。
      return [
        { uuid: 'diff', label: '时间差', componentType: 'FORM_NUMBER_INPUT' },
      ];
    },
  },
};
</script>
```

### 子表场景：`form-custom-table-field-assign-editor` 的 `otherTableFields`

场景是组件产出每行带多列的子表数据（例如给某个子表批量写值）。`otherTableFields` 描述**源子表的结构**：

```javascript
[{
  uuid: string,                           // 源子表 id
  label: string,                          // 源子表中文名
  componentType: 'FORM_WIDGET_SON_TABLE', // ⚠️ 第一级必须是子表类型
  children: [                             // 子表每一列的字段描述
    { uuid: 'col1', label: '列1',   componentType: 'FORM_NUMBER_INPUT' },
    { uuid: 'col2', label: '时间差', componentType: 'FORM_NUMBER_INPUT' },
    { uuid: 'col3', label: '备注',   componentType: 'FORM_TEXT_INPUT'   },
  ],
}]
```

**⚠️ 关键约束**：第一级元素的 `componentType` **必须**是 `'FORM_WIDGET_SON_TABLE'`；`children` 每项的 `componentType` 按上面的 componentType 映射表选。

### edit.vue 里怎么消费赋值配置（运行时写值）

`form-custom-field-assign-editor` 配置完后，`customComponentConfig.diffAssign` 存储格式为：

```javascript
[{
  origin: { uuid: 'diff', label: '时间差', componentType: 'FORM_NUMBER_INPUT' },
  target: { uuid: 'abc123-真实表单字段uuid', label: '结果字段', componentType: 'FORM_NUMBER_INPUT' },
}]
```

web 端和 mobile 端的 edit.vue 都要同样消费，算出派生值后按配置写到目标字段：

```javascript
watch: {
  formValue() { this.emitAssign(); },
},
methods: {
  emitAssign() {
    const diff = this.computeDiff();  // 自己算派生值
    const assigns = this.widget.customComponentConfig.diffAssign || [];
    for (const pair of assigns) {
      if (pair.origin.uuid === 'diff' && pair.target?.uuid) {
        this.$set(this.formData, pair.target.uuid, diff);
      }
    }
    if (this.formEngine?.formDataControl) {
      this.formEngine.formDataControl.ctlFormDataChanged = true;
    }
  },
}
```

**严禁**靠"aPaaS 字段联动规则"等外部机制实现赋值。自开发组件必须在 edit.vue 里通过 `this.$set(this.formData, targetUuid, value)` 直接写值。

### 独立 assign.vue（数据选择类组件，多赋值规则）

数据选择类组件赋值规则多时（主表 + 子表 + 清空行为），可拆出独立 `{name}-assign.vue`（同样只在 web 端）：

```javascript
// web/src/form-component/form-editor/index.js
import FormComponentDemoSetting from './form-component-demo-setting.vue';
import FormComponentDemoAssign from './form-component-demo-assign.vue';
const customFormConfigList = [FormComponentDemoSetting, FormComponentDemoAssign];
export default customFormConfigList;
```

对应 `web/src/form-component-config/form-editor/` 也要两份 `*.editor.config.json`。

## 🛑 自造业务 editor 的强制约定（严禁违反）

scaffold 6 个预置 editor 原子（input / select / textarea / switch / sechma-item + field-assign/table-field-assign）**覆盖绝大多数场景**。只有确认预置原子完全无法满足的业务 UI（例如日期范围、颜色选择、JSON 结构编辑器）才允许新建 `web/src/form-component/form-editor/components/form-custom-{业务名}-editor.vue`。

**必须严格按以下模式**，否则会出现"UI 显示正常但数据存不进去"的静默 bug：

```vue
<template>
  <form-custom-sechma-item
    :label="label"
    :property="property"
    :config-property="configProperty"
    :showRequired="showRequired"
    :help="help"
    :rules="rules"
  >
    <!-- 你的业务 UI，用 v-model="formValue" 读写值 -->
    <el-date-picker
      v-model="formValue"
      type="datetimerange"
      value-format="yyyy-MM-dd HH:mm:ss"
      @change="handleChange"
    />
  </form-custom-sechma-item>
</template>
<script>
import EditorFormConfigMixin from '@shared/mixin/form-config.mixin';
import FormEditorMixin from '@shared/mixin/form-editor.mixin';   // ⚠️ 必须 import
import FormCustomSechmaItem from './form-custom-sechma-item.vue';

export default {
  name: 'FormCustomDateTimeRangeEditor',
  mixins: [EditorFormConfigMixin, FormEditorMixin],             // ⚠️ 必须 mixins 两者
  components: { FormCustomSechmaItem },
  // 只声明业务特有 props（如日期格式、自定义 options 等）；
  // label / property / help / showRequired / placeholder 由 FormEditorMixin 提供，不要重复声明
};
</script>
```

**铁则**：

1. **必须** `mixins: [EditorFormConfigMixin, FormEditorMixin]`——`formValue` 由 FormEditorMixin 自动双向绑定到 `componentConfig[configProperty][property]`，**禁止**自己实现 computed getter/setter。
2. **禁止**通过 `this.$parent[configProperty]` 读配置（错误 API）。
3. **禁止**重复声明 `label` / `property` / `help` / `showRequired` / `placeholder` props，FormEditorMixin 已提供。
4. **必须**用 `<form-custom-sechma-item>` 包裹，透传 `label / property / configProperty / showRequired / help / rules` 6 个 prop。
5. 值变化时调用 `handleChange`（FormEditorMixin 提供）或让 `v-model="formValue"` 自动触发，**禁止** `$emit('update:componentConfig', ...)`。
6. 参照 scaffold 里 `form-custom-input-editor.vue` / `form-custom-select-editor.vue` 的结构，保持一致。
7. **双端专属**：import 必须用 `@shared/mixin/...`，不要用 `@/mixin/...`。

## 易错点清单

| ⚠️ 问题 | 正确做法 |
|---|---|
| setting.vue 放到了 `mobile/` 或 `shared/` | 必须放 `web/src/form-component/form-editor/`，设计器配置面板只跑在 web |
| 用 `@/mixin/...` | 双端必须用 `@shared/mixin/...` |
| 用 `this.widget.xxx` 读配置 | setting.vue 用 `this.componentConfig.customComponentConfig.xxx` |
| 数组直接 `push` 到 customComponentConfig | 先拷贝再 `this.$set` 赋值 |
| `onOk` 未 `return true` 导致意外自动关闭 | 需要内部控制关闭时机时 `return true` |
| setting.vue `name` 与 editor.config.json `componentName` 不一致 | 必须完全一致（PascalCase） |
| 漏注册到 `web/src/form-component/form-editor/index.js` 的 `customFormConfigList` | 必须导出 |
| 用 `.then/.catch` 写 `this.$request` | 必须 `asyncThen(ok, bizErr).asyncErrorCatch(netErr)` |
| 原子 editor 漏 `v-bind="$props"` | 导致 configProperty 丢失、校验失效 |
| 重写 `form-editor.mixin.js` 或 `form-custom-*-editor.vue` | scaffold 已预置，直接 import，不要重写 |
