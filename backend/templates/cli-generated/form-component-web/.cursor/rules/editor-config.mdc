---
description: editor.config.json 生成规则
globs:
  - "src/form-component-config/form-editor/*.editor.config.json"
alwaysApply: true
---

# editor.config.json 生成规则

## 文件格式

- 必须生成 **JSON 文件**，路径为 `src/form-component-config/form-editor/{name}.editor.config.json`
- 不要生成 `.editor.config.js`
- `index.js` 中导入必须带 `.json` 后缀：`import XxxEditorConfig from './{name}.editor.config.json'`

## ⚠️ 重要：此文件只有 4 个字段，不能放任何其他内容

`{name}.editor.config.json` 是一个**纯注册文件**，只负责告诉平台"有一个叫 X 的设置面板组件"。
**所有配置项（SELECT/INPUT/SWITCH 等）都必须写在 `setting.vue` 里，绝对不能写在这个文件里。**

```json
{
  "code": "FORM_CUSTOM_RATE_SETTING",
  "editorConfigType": "FORM_CUSTOM_RATE_SETTING",
  "componentName": "FormComponentRateSetting",
  "configProperty": "customComponentConfig"
}
```

- `code`：必须等于 `widget.config.json` 中顶层 `code` 字段 + `_SETTING`，例如 widget `code` 为 `FORM_CUSTOM_INTL_PHONE` 则此处为 `FORM_CUSTOM_INTL_PHONE_SETTING`。同时必须与 `widget.config.json` 的 `widget.editor.config` 数组末尾的自定义项一致
- `editorConfigType`：**与 `code` 完全相同的值**
- `componentName`：必须与 `setting.vue` 中的 `name` 选项完全一致
- `configProperty`：**固定值 `"customComponentConfig"`，不可修改**

此文件 **不允许** 出现 `editorConfigList`、`options`、`staticData`、`type`、`group` 等任何其他字段。

## 注册到 index.js

生成后必须将配置注册到 `src/form-component-config/form-editor/index.js`：

```js
import FormComponentRateEditorConfig from "./form-component-rate.editor.config.json"

const editorConfigList = [
  FormComponentRateEditorConfig
]

export default editorConfigList
```

## 与 setting.vue 的关联

- `componentName` 必须与 `src/form-component/form-editor/{name}-setting.vue` 中的 `name` 选项完全一致
- `setting.vue` 同时需要注册到 `src/form-component/form-editor/index.js`
