---
description: aPaaS 前端 SDK v2 速查规则
globs:
alwaysApply: true
---
# 前端 SDK v2 速查

## 适用范围

- 适用于 aPaaS 前端自开发场景：PC 组件、移动组件、PC 页面、移动页面、布局、插件等。
- `df-sdk` 依赖 aPaaS 宿主环境，脱离平台单独运行时不要假设 `window.df` 一定存在。

## 核心约束

1. 宿主通常已经注入 `window.df`、Vue、Element UI 等运行时能力，不要重复引入整套 UI 库。
2. 优先复用平台提供的 API，例如 `df.getVue()`、`df.getRouter()`、`df.getStore()`。
3. 涉及路由、全局状态、平台环境参数时，优先从 `df-sdk` 获取，而不是自己重新造一套全局对象。
4. 代码需要兼容平台宿主，避免写死浏览器独占逻辑或破坏平台已有全局样式。

## 常用 API

### `df.getVue()`

- 获取宿主 Vue 实例/构造器。
- 适合访问宿主已挂载的全局能力或做少量兼容适配。

### `df.getRouter()`

- 获取宿主路由对象。
- 适合做页面跳转、路由守卫、基于路由参数的初始化。

### `df.getStore()`

- 获取宿主状态管理对象。
- 常用于读取当前租户、用户、主题、应用配置等上下文。

## 开发建议

1. 优先按脚手架已有目录和导出方式补代码，不要擅自改入口协议。
2. 页面/组件里如果要读取平台上下文，优先通过 `df-sdk` 或宿主注入能力获取。
3. 如果项目里已经有 API 封装、mixin、i18n 目录，优先沿用，不要旁路新增一套风格完全不同的实现。
4. 当需求涉及“平台能力”时，先检查 `src/apaas.json`、`src/index.js`、`src/api/`、`src/mixin/` 等文件再下手。

## 生成代码时的默认偏好

- 默认输出中文注释和中文回答。
- 优先写可直接在 aPaaS 宿主里运行的代码，不要假设纯本地浏览器环境。
- 若平台已全局注册组件或方法，不要重复 import。
