---
description: 
globs: 
alwaysApply: true
---
# 自开发菜单页面AI提示词

## 项目说明

这是一个得帆低代码自开发脚手架基于Vue2.7版本生成的自开发菜单页面的工程，并遵循最佳实践进行目录组织和配置管理，开发者可以在此基础上快速开展业务开发。

### 项目结构指南

本项目是一个前端项目，使用了 Vue.js2.7 (基于 `vue.config.js` 和 `babel.config.js` 的存在)。

### 详细目录结构

```
.
├── https/                             // 可能包含 HTTPS 相关的证书文件，用于本地开发服务器
├── public/                            // 公共静态资源目录
│   └── form-page/                     // 表单页面相关静态资源
│       └── form-page-demo/            // 预留静态资源子目录（当前为空）
├── src/                               // 源代码目录
│   ├── index.js                       // 入口 JS 文件
│   ├── apaas.json                     // aPaaS自开发菜单页面 相关配置文件
│   ├── api/                           // API 请求相关代码
│   │   └── index.js                   // API 文件
│   ├── mixin/                         // Vue Mixin 目录
│   │   └── custom-permissions.mixin.js// 自定义权限 Mixin
│   ├── form-page/                     // 表单页面相关 Vue 组件
│   │   └── apaas-custom-demo.vue      // 表单页面自定义示例组件
│   └── form-page-local/               // 表单页面本地化资源
│       ├── index.js                   // 本地化资源入口
│       ├── zh-CN/                     // 中文本地化目录
│       │   └── index.js               // 中文本地化内容
│       └── en-US/                     // 英文本地化目录
│           └── index.js               // 英文本地化内容
```

### 核心目录说明

- src/api/：用于封装所有后端接口调用，推荐使用统一请求工具并按业务模块分类。
- src/form-page/：具体的菜单页面代码。
- src/form-page-local/：支持国际化的多语言文案定义。
- src/mixin/：平台提供的通用 mixin，用于简化组件开发。
- src/apaas.json：该文件定义当前菜单页面的元数据，是菜单页面与平台对接的桥梁。
- src/index.js：整个组件模块的入口文件，负责导出组件能力供平台使用。

## 实现代码示例

提供了自开发菜单页面相关的代码示例，生成的代码可以参考示例中的代码结构和逻辑。

### 自开发菜单页面

在低代码平台中，自开发菜单页面是指开发者基于平台能力，自主实现自定义的菜单页面。平台提供了统一的组件声明机制、生命周期规范以及组件加载方式，确保组件可以无缝集成。

#### 菜单页面开发位置说明

菜单页面组件主要集中在以下目录：

- `src/form-page/`：用于存放具体的组件实现代码；
- `src/apaas.json`：用于注册菜单页面的元信息（入口文件、资源文件、菜单路由等）；
- `src/index.js`：用于统一导出所有组件，供平台加载；

#### 菜单页面vue组件代码示例

src/form-page/apaas-custom-demo.vue
```
<template>
  <div class="apaas-custom-demo">
    <x-ag-grid
      rowKey="id"
      :tableData="tableData"
      :colConfigs="colConfigs"
      :pagination="pagination"
      @size-change="onSizeChange"
      @current-page-change="onCurrentPageChange"
    ></x-ag-grid>
  </div>
</template>

<script>
import Api from "../api";
export default {
  data() {
    return {
      tableData: [],
      colConfigs: [
        {
          headerName: "账号",
          field: "account",
        },
        {
          headerName: "工号",
          field: "userNumber",
        },
        {
          headerName: "用户名",
          field: "username",
        },
        {
          headerName: "部门名称",
          field: "deptName",
          showOverflowTooltip: true,
        },
        {
          headerName: "主部门",
          colId: "mainDepartment",
          valueGetter: (params) => params.data.mainDepartment?.name,
          flex: 1,
        },
      ],
      pagination: {
        currentPage: 1,
        pageSize: 10,
        total: 0,
      },
    };
  },
  created() {
    this.getTableData();
  },
  methods: {
    onSizeChange(size) {
      this.pagination.pageSize = size;
      this.getTableData();
    },
    onCurrentPageChange(page) {
      this.pagination.currentPage = page;
      this.getTableData();
    },
    getTableData() {
      const { currentPage, pageSize } = this.pagination;
      this.$request({
        ...Api.QUERY_ALL_USERS,
        params: {
          page: currentPage,
          pageSize,
        },
      })
        .asyncThen(
          (resp) => {
            this.tableData = resp.table;
            this.pagination.total = resp.total;
          },
          (error) => {
            console.error(error);
          }
        )
        .asyncErrorCatch((error) => {
          console.error(error);
        });
    },
  },
};
</script>

<style lang="scss">
.apaas-custom-demo {
  box-sizing: border-box;
  padding: 20px;
}
</style>
```

### 其他代码示例补充

这里对于一些常用的需求代码进行示例补充

### 其他要求补充
1. 使用ElementUI作为UI库进行开发，但是不引入，宿主容器已经有Element UI的全部组件
2. 尽可能遵守已有文件内容，补充相关代码。如果为空则根据规范编写
3. 不要随意更改已有的配置内容和code
4. 特别注意进行接口请求时的构造方式，参考如下代码。URL参数拼接在url上，并覆盖，body参数直接将json体放到参数位置上即可
```js
this.$request({
        ...Api.GET_ORDER_LIST,
        url: Api.GET_ORDER_LIST.url + `?pageNum=${currentPage}&pageSize=${pageSize}`,
        params
      })
        .asyncThen(
          (resp) => {
            if (resp.code === "ok") {
              this.tableData = resp.table || [];
              this.pagination.total = resp.total || 0;
            } else {
              this.$message.error(resp.message || "获取订单列表失败");
            }
          },
          (error) => {
            console.error("获取订单列表失败", error);
            this.$message.error("获取订单列表失败");
          }
        )
        .asyncErrorCatch((error) => {
          console.error("获取订单列表请求异常", error);
          this.$message.error("获取订单列表请求异常");
        });
    },
```
