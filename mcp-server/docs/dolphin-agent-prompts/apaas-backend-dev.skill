---
name: apaas-backend-dev
description: 开发 aPaaS 低代码二次开发后端自开发包时使用。负责 4/5 版本选择、JDK 规则、后端模板、Maven 打包、单独打包和发布；不用于 MCP 搭建。
---

# aPaaS 后端开发 Skill

## 先问版本

用户要做后端自开发时，先确认 aPaaS 版本：

- `4 版本`：JDK `1.8`
- `5 版本`：JDK `17`

不要说 JDK 21。不要把 5 版本写成 JDK 8。

## 项目类型

后端自开发主要有三类：

- `backend-api`：后端接口
- `backend-feign`：Feign 外部调用
- `backend-scheduled`：定时任务

这些都是后端 jar，不是前端源码 zip。平台类型走 `BACKENDENGINE`。

## 4 版本规则

- Java：JDK `1.8`
- Spring Boot：`2.2.7.RELEASE`
- `papaas.version`：通常 `4.1.1-rc`
- 依赖：`motor-spring-boot-starter`、`app`、`runtime`
- 上传用 profile：`lib`
- 打包命令：`mvn clean package -P lib -DskipTests`
- 产物应该是薄 jar，通常没有 `BOOT-INF/lib`

## 5 版本规则

- Java：JDK `17`
- Spring Boot：`3.5.8`
- `papaas.version`：通常 `5.0.0`
- Spring Cloud：`2025.0.0`
- Spring Cloud Alibaba：`2025.0.0.0`
- 依赖：`com.xdap:app`、`com.xdap:runtime`
- 仓库：`https://registry.dfy.definesys.cn/repository/maven-public/`
- profile：
  - `lib`：平台上传用，排除 `properties/yml`
  - `single`：本地可运行调试用

5 版本 pom 的核心形态：

```xml
<parent>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-parent</artifactId>
  <version>3.5.8</version>
  <relativePath/>
</parent>
<properties>
  <spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>
  <spring-cloud.version>2025.0.0</spring-cloud.version>
  <papaas.version>5.0.0</papaas.version>
</properties>
```

## 推荐工作流

1. 先问用户：`你做 4 版本还是 5 版本？`
2. 根据版本选模板：4 用旧模板，5 用新模板。
3. 生成或修改 workspace。
4. 只验证打包时用 `build_dev_workspace`。
5. 用户确认要上线时才用 `publish_dev_workspace`。
6. 打包失败先看环境：主服务镜像是否有对应 JDK 和 Maven。

## 工具选择

- 查应用、表单、模型：用 `apaas-builder-coding` 里的查询工具。
- 创建 workspace：`create_dev_workspace`。
- 读写代码：`read_workspace_file`、`write_workspace_files`。
- 单独打包：`build_dev_workspace`。
- 发布到 aPaaS：`publish_dev_workspace`。
- 任意命令只在必要时用 `run_workspace_command`。

## 后端打包结论

低代码后端打包现在是主服务直跑，不是独立沙箱。主服务镜像必须有 Java 和 Maven。

当前版本规则：

- 4 = JDK 1.8
- 5 = JDK 17
