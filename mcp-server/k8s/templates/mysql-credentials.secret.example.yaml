# Example only. Do not apply this file as-is.
#
# Create the real Secret before applying k8s/10-mysql.yaml:
#
# kubectl -n apaas-mcp-server create secret generic mysql-credentials \
#   --from-literal="MYSQL_ROOT_PASSWORD=<strong-root-password>" \
#   --from-literal="MYSQL_USER=apaas_builder" \
#   --from-literal="MYSQL_PASSWORD=<strong-app-password>" \
#   --from-literal="MYSQL_DATABASE=apaas_builder" \
#   --dry-run=client -o yaml | kubectl apply -f -

apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
  namespace: apaas-mcp-server
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "PLACEHOLDER_ROOT_PASSWORD_CHANGE_ME"
  MYSQL_USER: "apaas_builder"
  MYSQL_PASSWORD: "PLACEHOLDER_APP_PASSWORD_CHANGE_ME"
  MYSQL_DATABASE: "apaas_builder"
