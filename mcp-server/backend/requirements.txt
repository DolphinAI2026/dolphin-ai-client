fastapi==0.115.0
uvicorn[standard]==0.32.0
# 2026-05-13: 放宽下限到 2.11 — mcp[cli]>=1.27 要求 pydantic>=2.11；
# fastapi 0.115 / pydantic-settings 2.6 跟 2.11 兼容（向后兼容 2.x ABI）
pydantic>=2.11.0,<3.0.0
pydantic-settings==2.6.0
sqlalchemy==2.0.36
aiomysql==0.2.0
aiosqlite==0.20.0
greenlet==3.2.5
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
httpx==0.27.2
sse-starlette==2.1.3
cryptography==43.0.1
playwright>=1.49.0
python-docx
pytest>=8.0.0
pytest-asyncio>=0.23.0

# MCP server (used by app/mcp_server.py — 暴露应用领域能力给 dolphin / 其他 agent 平台)
# 2026-05-13: pin 到 <1.20 — mcp 1.27 streamable_http_manager.py:125 anyio cancel scope
# 跨 task 退出的 regression bug，跟 uvicorn lifespan + AsyncExitStack 嵌套两个 FastMCP
# session_manager.run() 不兼容。
mcp[cli]>=1.10.0,<1.20.0

# K8s python client (used by app/vibe_coding/k8s_runtime.py — vibe-coding pod-per-workspace 沙箱)
kubernetes-asyncio>=31.0.0
