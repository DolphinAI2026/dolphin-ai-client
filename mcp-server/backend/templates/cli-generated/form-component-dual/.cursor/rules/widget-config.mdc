---
description: shared/widget.config.js 生成规则（双端模板专用）
globs:
  - "shared/widget.config.js"
alwaysApply: true
---

# shared/widget.config.js 生成规则

## 文件位置与格式

- 文件路径：`shared/widget.config.json`（**JSON 格式，非 JS**）
- PC 端和移动端都从此文件 import，**禁止在 web/ 或 mobile/ 中各自维护一份**
- 不要生成 `.widget.config.js`

web 端引用方式：
```js
// web/src/form-component-config/form-widget/index.js
import FormComponentXxxWidgetConfig from '@shared/widget.config.json'
```

mobile 端引用方式相同：
```js
// mobile/src/form-component-config/form-widget/index.js
import FormComponentXxxWidgetConfig from '@shared/widget.config.json'
```

## 顶层必填字段

以下字段缺一不可：
```
version / code / desc / instance / component / widget / client / componentModelField / methods / formatValueSchema
```

## 顶层必填字段

以下字段缺一不可：
```
version / code / desc / instance / component / widget / client / componentModelField / methods / formatValueSchema
```

## code

- 必须以 `FORM_CUSTOM_COMPONENT_` 开头，后接语义化大写英文，例如 `FORM_CUSTOM_COMPONENT_INTL_PHONE`
- 必须与 `web/src/apaas.json` 和 `mobile/src/apaas.json` 中的 `code` 字段保持一致

## desc

```js
desc: {
  iconType: 'DEFAULT',
  icon: '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">...</svg>',
  text: '组件真实名称',
  description: '组件真实描述'
}
```

- `iconType`：固定值 `'DEFAULT'`，不可修改
- `icon`：**必须是与组件语义匹配的 SVG 字符串**
- `text` / `description`：**必须填写真实内容，禁止出现 Demo、demo 等占位文字**

## instance

固定值，不可修改：
```js
instance: { uuid: '$itemUuid', inTable: false }
```

## component（PC 端）

```js
component: {
  ide: 'FormComponentXxxIde',
  edit: 'FormComponentXxxEdit',
  read: 'FormComponentXxxRead',
  list: 'FormComponentXxxList',
  print: 'FormComponentXxxPrint',
  search: 'FormComponentXxxSearch',
  searchIde: 'FormComponentXxxSearchIde'
}
```

- 必填：`ide` / `edit` / `read`
- 可选：`list` / `print` / `search` / `searchIde` / `association` / `lov`

## client.mobile.component（移动端）

```js
client: {
  mobile: {
    component: {
      ide: 'MobileFormComponentXxxIde',
      edit: 'MobileFormComponentXxxEdit',
      read: 'MobileFormComponentXxxRead',
      list: 'MobileFormComponentXxxList',
      print: 'MobileFormComponentXxxPrint',
      search: 'MobileFormComponentXxxSearch',
      searchIde: 'MobileFormComponentXxxSearchIde'
    },
    widget: {
      editor: {
        config: [ /* 同 widget.editor.config */ ],
        excludeInTable: ['WIDTH']
      }
    }
  }
}
```

- 命名规则：`Mobile` + PC 端组件名
- 必填：`ide` / `edit` / `read`

## widget.display

```js
display: {
  label: '组件真实名称',   // 与 desc.text 保持一致
  width: 6,              // 只能是 3 / 6 / 12
  mobileWidth: 12,       // 只能是 6 / 12
  height: 1,
  hidden: false,
  readOnly: false,
  required: false,
  onlyCreateEdit: false
}
```

## widget.allow

四个字段必须全部显式写出：
```js
allow: {
  calcRule: false,
  useInTableColumn: true,
  scanCode: false,
  copy: false
}
```

## widget.default

```js
default: { customDefaultKey: 'defaultValue', value: null }
```

- `value` 必须是 `null`，**不能是 `''` 或其他值**

## widget.editor.config

标准顺序，自定义配置项插入在 `WIDTH` 之后：
```js
config: [
  'INFO', 'LABEL', 'FIELD_CODE', 'TITLE_DESCRIPTION', 'WIDTH',
  /* 自定义项，如 'FORM_CUSTOM_COMPONENT_XXX_SETTING' */
  'HIDDEN', 'READONLY', 'REQUIRED', 'EDITONNEW',
  'UNIQUE', 'HIDDEN_SAVE', 'HIDDEN_TRIGGER', 'TRIGGER_BUSINESS_EVENTS'
]
```

- `FORMULA_RULE` 仅在需要公式规则时加入，默认不加
- `excludeInTable` 必须包含 `'WIDTH'`

## componentModelField

| 存储内容 | 值 |
|---|---|
| 字符串 < 500 字符 | `['STRING']` |
| 字符串 ≥ 500 字符 | `['BIG_TEXT']` |
| 数字 | `['NUM']` |
| 单个日期 | `['DATE']` |

## methods / formatValueSchema

固定为空对象：
```js
methods: {},
formatValueSchema: {}
```
