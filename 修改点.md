# 资源更新现状记录

## 1. 总体策略

当前资源更新走的是“本地配置快照驱动的增量更新”。

- 前端先基于当前 `store.preview` 组装 `new_config`
- 后端读取应用表里的 `config_preview` 作为 `old_config`
- 后端再拉一次平台远端资源，用于补充 `remote_id`
- 之后做差异计算，生成 `diff`
- 最后按固定顺序执行增量更新：`角色 -> 字典 -> 模型 -> 表单 -> 流程`
- 执行成功后，回写本地 `config_preview`

代码位置：

- `backend/app/routes/incremental_update.py`
- `backend/app/config_diff.py`
- `backend/app/incremental_executor.py`
- `frontend/src/views/ChatPage.vue`

### 1.1 当前增量更新入口接口

当前前后端对外暴露的增量更新入口如下：

- 计算差异：`POST /api/applications/{app_id}/incremental/diff`
- 预览变更：`POST /api/applications/{app_id}/incremental/preview`
- 非流式执行：`POST /api/applications/{app_id}/incremental/execute`
- 流式执行：`GET /api/applications/{app_id}/incremental/execute-stream`
- 获取平台远端数据：`GET /api/applications/{app_id}/incremental/remote-data`

## 2. 各类资源当前更新策略

### 2.1 角色

- 识别键：角色名称，使用 `roleName/name` 作为唯一标识
- 新增：旧配置无、新配置有，则创建
- 删除：旧配置有、新配置无，则删除
- 修改：仅当 `roleCode/code` 变化时，才视为修改
- 平台接口（当前代码实际使用）：
  - 查询角色列表：`POST /xdap-app/roles/query/rolesList`
  - 创建角色：`POST /xdap-app/common/resource/appRole`
  - 更新角色：`POST /xdap-app/roles/edit/role`
  - 删除角色：`POST /xdap-app/roles/delete/role`

结论：

- 当前角色策略本质上是“按名称定位，按编码变化触发更新”

### 2.2 数据字典

- 识别键：字典编码，使用 `dictionaryCode/code`
- 字典本身的修改检测：
  - 主要比较字典名称是否变化
- 字典选项的识别键：
  - 使用 `valueCode/code`
- 字典选项的修改检测：
  - 主要比较选项名称是否变化
- 执行策略：
  - 字典新增：创建
  - 字典修改：更新字典本体，再处理选项变更
  - 字典删除：不直接删，改为禁用
  - 选项删除：不直接删，改为禁用
- 平台接口（当前代码实际使用）：
  - 查询字典列表：`POST /xdap-app/dataDictionary/query/dataDictionaryList`
  - 查询字典选项：`POST /xdap-app/dataDictionary/query/dictionaryValueList`
  - 创建字典：`POST /xdap-app/common/resource/appDict`
  - 更新字典：`POST /xdap-app/dataDictionary/edit/dataDictionary/fromApp`
  - 禁用字典：`GET /xdap-app/dataDictionary/disable/dataDictionary?id={id}`
  - 新增字典选项：`POST /xdap-app/dataDictionary/add/dictionaryValue`
  - 更新字典选项：`POST /xdap-app/dataDictionary/edit/dictionaryValue/fromApp`
  - 禁用字典选项：`GET /xdap-app/dataDictionary/disable/dictionaryValue?id={id}`
- 平台接口（规划里有，但当前增量执行代码未使用）：
  - 启用字典：`GET /xdap-app/dataDictionary/enable/dataDictionary?id={id}`
  - 启用字典选项：`GET /xdap-app/dataDictionary/enable/dictionaryValue?id={id}`

结论：

- 当前字典策略是“按编码定位，名称变化触发更新，删除走禁用”

### 2.3 模型

- 识别键：模型编码，使用 `modelCode/code`
- 模型本身的修改检测：
  - 主要比较模型名称
- 字段识别键：
  - 使用 `fieldCode/code`
- 字段修改检测：
  - 仅在 `fieldName` 或 `fieldComment` 变化时，才视为字段更新
  - 当前后端兼容 `fieldComment` / `description` 两种备注写法
- 远端 ID 读取：
  - 模型 `remote_id` 统一兼容 `id` / `modelId`
  - 字段 `remote_id` 统一兼容 `id` / `fieldId`
- 执行策略：
  - 模型新增：创建
  - 模型修改：更新模型本体，再处理字段变更
  - 模型删除：平台不支持，直接忽略并给 warning
  - 字段删除：不直接删，改为 `fieldStatus=DISABLE`
- 平台接口（当前代码实际使用）：
  - 查询模型（含字段）：`POST /xdap-app/dataModel/query/modelWithField`
  - 创建模型：`POST /xdap-app/common/resource/v2/appModel`
  - 更新模型：`POST /xdap-app/dataModel/update`
  - 新增字段：`POST /xdap-app/modelField/add`
  - 更新字段：`POST /xdap-app/modelField/update/fromApp`
  - 禁用字段：`POST /xdap-app/modelField/update/fromApp`，通过 `fieldStatus=DISABLE`
- 平台接口（规划里有，但当前增量执行代码未使用）：
  - 分页查询模型：`POST /xdap-app/dataModel/query/list`
  - 分页查询字段：`POST /xdap-app/modelField/query`
  - 批量更新字段：`POST /xdap-app/modelField/batchUpdate`

结论：

- 当前模型策略是“按编码定位，字段增删改单独处理，模型删除不支持”

### 2.4 表单

- 设计上的识别键：表单编码，使用 `formCode/code`
- 组件识别策略：
  - 普通组件：用 `modelField`
  - 子表组件：用 `tableModelCode`
  - 子表内组件：仍用 `modelField`，并补一个所属子表标记
- 组件修改检测：
  - 仅比较一组有限属性，例如 `label`、`required`、`readOnly`、`hidden`、`placeholder`、`dictionarySelectConfig`、`tableColumn` 等
- 执行策略：
  - 不是逐组件 patch
  - 而是先查询完整表单 `simpleFormConfig`
  - 再整体替换 `detailPage.formComponents`
  - 最后调用保存接口全量回写
- 平台接口（当前代码实际使用）：
  - 查询菜单/表单列表：`POST /xdap-app/menu/query/manageAppMenu`
  - 创建表单配置：`POST /xdap-app/common/resource/formConfig`
  - 查询完整表单配置：`GET /xdap-app/v2/form/query/formContext?formId={formId}`
  - 保存表单配置：`POST /xdap-app/formConfig/save/formConfigDetail`
  - 删除表单：`POST /xdap-app/menu/delete/menu`
- 平台接口（在客户端里存在，但当前增量执行主路径未直接使用）：
  - 创建菜单：`POST /xdap-app/menu/save/menu`
  - 更新单个组件辅助方法：内部仍走 `查询完整表单 -> 修改 -> 保存表单配置`

结论：

- 当前表单策略是“diff 看组件，但执行是整表单全量保存”

## 3. 当前已确认的问题

### 3.1 角色修改检测过窄

当前角色 diff 只在编码变化时才认定为修改。

这会导致以下变更可能检测不到：

- `roleParams`
- `enableGroupParam`
- 其他角色配置属性

影响：

- 同名角色如果只是权限参数变了，当前增量更新不会触发

### 3.2 字典修改检测过窄

当前字典和字典选项的修改检测主要只看名称。

这会漏掉：

- 字典描述变化
- 多色状态变化
- 选项排序变化
- 选项颜色变化
- 其他扩展属性变化

影响：

- 配置上看起来“改了”，diff 仍可能判定为无变化

### 3.3 模型字段备注字段名仍未完全统一

当前字段更新判定和执行已经兼容 `fieldComment` / `description` 两种写法，但前端类型定义和不同来源配置里的字段命名仍不完全统一。

影响：

- 同一份“字段备注”在不同链路里可能叫 `fieldComment`，也可能叫 `description`
- 目前后端已兼容，但整体 schema 仍建议统一

### 3.4 表单增量更新链路基本未接通

前端当前的 `PreviewData` 里没有 `forms` 字段，提交增量更新时传给后端的也是 `store.preview`。

这意味着：

- 后端 `compute_config_diff()` 在大多数情况下拿到的 `new_config.data.forms` 是空数组
- `form_changes` 可能长期无法正确产生

影响：

- 代码里虽然实现了表单 diff / execute 逻辑
- 但从前端当前实际传参看，表单增量更新大概率不会真正触发

### 3.5 表单远端 ID 使用存在风险

当前表单更新逻辑使用的是 `change.remote_id` 去查询表单配置，但查询表单配置接口要求的是 `formId`。

而远端表单数据当前来自菜单查询，diff 中又把菜单项的 `id` 记到了 `remote_id`。

影响：

- 更新表单时，可能把菜单 `id` 当成表单 `formId`
- 从而导致查询表单配置失败，表单更新被跳过

### 3.6 表单执行是全量覆盖，不是精确 patch

虽然 diff 里识别了组件级变更，但执行时是整块替换 `detailPage.formComponents`。

影响：

- 只改一个组件，也会整体覆盖表单组件区
- 如果平台表单上已经有本地快照之外的人工调整，可能被覆盖掉

### 3.7 流式增量更新对本地配置回写过于激进

非流式执行只有在 `result.success` 为真时才会回写 `config_preview`。

但流式执行当前在收到 `complete` 事件后，会直接回写本地配置，没有额外判断整体是否完全成功。

影响：

- 如果执行过程里存在部分失败，前端主流程使用的 SSE 仍可能把“未完全成功的新配置”写回本地
- 后续 diff 基准会被污染

### 3.8 接口层面还存在一处认知断层

虽然文档和代码里已经补齐了角色、字典、模型、表单的大部分执行接口，但“有接口”不等于“链路已经接通”。

当前仍然存在：

- 表单接口齐全，但前端 preview 未传 `forms`
- 表单查询接口要求 `formId`，当前 diff 里记录的远端标识存在取错风险

影响：

- 现在的问题更多是 schema 对齐和 ID 映射问题，不是单纯缺接口

## 4. 当前策略的真实状态判断

从代码现状看：

- 角色：可用，但修改检测偏弱
- 字典：基本可用，但属性级 diff 不完整
- 模型：基本可用，但字段类型识别受 preview/schema 不一致影响
- 表单：代码有实现，但前后端链路和 ID 映射都存在明显问题，当前不应视为稳定可用

## 5. 后续建议

建议后续按以下顺序修正：

1. 统一 preview schema 与增量 diff 使用的字段结构
2. 给前端 preview 显式补上 `forms`
3. 修正表单远端映射，明确区分 `menuId` 与 `formId`
4. 扩大角色、字典、字段的属性级 diff 覆盖范围
5. 修正 SSE 流式执行完成后的本地配置回写条件
